// Fix: Removed the unresolved type reference to "vite/client".
// The application does not use Vite-specific client features that require these types,
// so removing the reference resolves the compilation error without side effects.
